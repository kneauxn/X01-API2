const COLLECTION = 'users';

export default class {
  
  constructor(db) {
    this.collection = db[COLLECTION];
  }
  
  
  all() {
    
  }
  
  byId(id) {
    
  }
  
  create(user = {}) {
    
  }
  
  update(id, user = {}) {
    
  }
  
  remove(id) {
    
  }
};
